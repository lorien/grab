MONGODB_CONNECTION = {
    # key must be "database"
    "database": "grab_test",
}

MYSQL_CONNECTION = {
    # key must be "database"
    "database": "grab_test",
    # driver specific keys
    "host": "127.0.0.1",
    "port": 3306,
    "user": "github",
    "passwd": "github",
}

REDIS_CONNECTION = {}

POSTGRESQL_CONNECTION = {
    # key must be "database"
    "database": "grab_test",
    # driver specific keys
    "host": "127.0.0.1",
    "port": 5432,
    "user": "github",
    "password": "github",
}
